<view class="i-panel i-class" style="z-index: {{zIndex}};">
    <view wx:if="{{ title }}" class="i-panel-title {{ hideTop ? 'i-panel-title-hide-top' : '' }}">{{ title }}</view>
    <view class="i-panel-content {{ hideBorder ? 'i-panel-without-border' : '' }}" style="{{ hideBorder ? 'border:0px;' : '' }}"><slot></slot></view>
</view>
